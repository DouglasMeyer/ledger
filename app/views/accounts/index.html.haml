%h1 Accounts

= link_to "New Account", new_account_path

.accounts
  %table.assets
    %caption Assets
    = render @accounts.assets

  %table.liabilities
    %caption Liabilities
    = render @accounts.liabilities
